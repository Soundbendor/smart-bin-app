name: waste_watchers
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  path: ^1.8.3
  sqflite: ^2.3.0
  wifi_scan: ^0.4.0+2
  wifi_iot: ^0.3.18+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
